# NBA Phase 3D2B frozen private player-shell import contract

Status: first Atlanta data-store write

## Authorized write

Phase 3D2B may append exactly 442 player identity shells authorized by the
corrected Phase 3D1 freeze SHA-256:

`42AF200DAE6524919DED3CBDA62BDFE2238D3818F3119AD99B99499C5A404238`

The guarded pre-import player store contains 67 records. The post-import store
must contain exactly 509 records.

## Identity rules

Every imported player must use the exact frozen:

- player ID;
- preferred name;
- normalized name;
- slug;
- source variants;
- per-player freeze hash.

No fuzzy matching, external-ID lookup, alias inference, or automatic merge is
authorized.

Seven existing-player matches remain reuse-only and are not rewritten.

## Relationship staging

The 442 shells carry exactly 549 frozen player-trade references as
`pendingSourceReferences`.

Active relationship fields remain empty:

- `referenceCount = 0`;
- `sourceTradeCount = 0`;
- `sourceTradeIds = []`;
- `canonicalTradeIds = []`;
- `sourceReferences = []`.

No pending reference may become active until its canonical trade is committed
in a later guarded phase.

## Privacy

Every imported shell remains:

- private;
- noindex;
- ad-free;
- publication-ineligible;
- external-identity-unverified;
- automatic-merge-disabled.

Private local player pages may be generated by the existing data-driven Astro
routes. They remain noindex, ad-free, sitemap-excluded, navigation-excluded,
and unavailable from public pages.

## Idempotence

The importer must be run twice with the same timestamp and checkpoint. The
second run must produce zero changes and verify byte-identical player-store and
receipt content.

## Prohibited actions

Phase 3D2B may not:

- modify `trades.json`;
- import canonical trades;
- activate relationship edges;
- reconcile the Trae Young perspective;
- infer asset routing;
- push;
- create a preview deployment;
- deploy to production.
