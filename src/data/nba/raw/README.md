# Raw NBA intake

This directory stores immutable source submissions after the intake phase begins.

Rules:

- Never edit a raw submission in place.
- Every submission receives a stable `submissionId` and SHA-256 `contentHash`.
- Corrections arrive as new submissions linked to the prior source record.
- No raw trade records are included in Phase 2A.
